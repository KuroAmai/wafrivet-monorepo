import { SettingsPanel } from "@/components/settings/SettingsPanel";

export const metadata = {
  title: "Settings | Wafrivet",
};

export default function SettingsPage() {
  return <SettingsPanel />;
}
